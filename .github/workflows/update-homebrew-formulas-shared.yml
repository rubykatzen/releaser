name: Update Homebrew Formulas
"on":
  workflow_call:
jobs:
  update:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
      - uses: rubykatzen/releaser/.github/actions/update-homebrew-formulas@fix/homebrew-formula-path
