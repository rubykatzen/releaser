name: Publish release
on:
  pull_request:
    types: [closed]
    branches: [main]
jobs:
  publish:
    if: github.event.pull_request.merged == true && startsWith(github.event.pull_request.head.ref, 'release/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: rubykatzen/baseline/.github/actions/read-release-data@v0.3.0
        id: meta
        with:
          branch: ${{ github.event.pull_request.head.ref }}
      - uses: rubykatzen/baseline/.github/actions/create-release@v0.3.0
        with:
          version: ${{ steps.meta.outputs.version }}
          notes: ${{ steps.meta.outputs.notes }}
  update-tap:
    needs: publish
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Homebrew tap update
        env:
          GH_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          RELEASE_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          version="${RELEASE_BRANCH#release/v}"
          gh workflow run update-formulas.yml \
            --repo rubykatzen/homebrew-tap \
            --field version="${version}"
