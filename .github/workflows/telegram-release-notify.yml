name: Telegram Release Notify
on:
  schedule:
    - cron: "0 22 * * *"
  workflow_dispatch:
jobs:
  notify:
    uses: rubykatzen/baseline/.github/workflows/telegram-release-notify.yml@v0.0.12
    secrets: inherit
