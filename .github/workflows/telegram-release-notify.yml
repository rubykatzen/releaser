name: Telegram Release Notify
on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:
jobs:
  notify:
    uses: rubykatzen/baseline/.github/workflows/telegram-release-notify.yml@v0.0.8
    secrets: inherit
