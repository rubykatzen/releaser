name: Auto-merge Dependabot
on:
  pull_request_target:
    types: [opened, reopened, synchronize]
jobs:
  merge:
    uses: rubykatzen/baseline/.github/workflows/auto-merge-dependabot.yml@v0.0.7
    secrets: inherit
